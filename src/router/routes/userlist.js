import {UsersList} from "../../views/apps/user/list/Table"

const Routes = [
  {
    path: "/userlist",
    exact: true,
    component: UsersList,
    element: <UsersList />
  }
]

export default Routes
